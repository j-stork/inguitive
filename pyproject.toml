[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "inguitive"
version = "1.0.1"
description = "A pure Python UI layer for FastAPI: compose pages from Python components instead of templates, with reactive state and SSE-driven partial updates via HTMX"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    {name = "Johannes Stork", email = "info@stork-software.de"}
]
keywords = ["web", "ui", "htmx", "tailwind", "fastapi", "python", "components", "sse"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Framework :: FastAPI",
    "Topic :: Internet :: WWW/HTTP",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

dependencies = [
    "fastapi>=0.95.0",
    "jinja2>=3.1.0",
    "markupsafe>=2.0.0",
    "python-multipart>=0.0.6",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
    "mypy>=1.0.0",
    "httpx>=0.24.0",
    "uvicorn[standard]>=0.21.0",
]
redis = [
    "redis>=4.2",
]
docs = [
    "mkdocs>=1.5",
    "mkdocs-material>=9.0",
    "mkdocstrings[python]>=0.24",
    "mkdocs-autorefs>=0.5",
]

[project.urls]
Homepage = "https://github.com/j-strk/inguitive"
Documentation = "https://github.com/j-strk/inguitive/blob/main/README.md"
Repository = "https://github.com/j-strk/inguitive"
Issues = "https://github.com/j-strk/inguitive/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"*" = ["*.svg", "py.typed"]
"inguitive" = ["static/*"]

[tool.black]
line-length = 100
target-version = ["py310", "py311", "py312"]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true

[tool.pytest.ini_options]
python_files = ["test_*.py", "*_test.py"]
addopts = "-v --tb=short"
testpaths = ["tests"]

